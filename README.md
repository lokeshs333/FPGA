# FPGA
learning the fpga from scratch
🔹 Intuition (this hits different 🧠)

Think of it like:

Baud rate = how many boxes you send per second 📦
Bit rate = how much stuff is inside each box

If each box carries more items → total data increases without increasing speed
